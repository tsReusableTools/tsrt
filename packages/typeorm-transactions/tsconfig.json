{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
  },
  "include": [
    "src/**/*.ts",
    "tests/**/*.ts" // Just for tests type and config inheritance
  ],
  "exclude": [
    "tests/**/*.ts" // Just for tests type and config inheritance
  ]
}
